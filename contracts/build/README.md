# This directory contains compiled TEAL and ABI JSON output.
# Run `python contracts/app.py` to populate it.
# Files generated here:
#   approval.teal   — approval program TEAL
#   clear.teal      — clear state program TEAL
#   contract.json   — ARC-4 ABI descriptor (used by wallet_manager.ts + transactionBuilder.js)
